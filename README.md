## A preliminary attempt at using RAG on Alpha


## Pre Requisites
- Expects `llamaindex` to be installed
- Expects `GITHUB_TOKEN` and `OPENAI_API_KEY` to contain a GitHub Token and an OpenAPI token


